# Web service: Root Directory MUST be "frontend".
# Uses THIS folder's Dockerfile only — not the repo-root image (which COPYs backend/).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["frontend/**"]

# Serve is started from Dockerfile CMD; set VITE_API_BASE_URL in Railway Variables for production API URL.
