# Web service: Root Directory = frontend. Set "Config as code" to /frontend/railway.toml in Railway.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["/frontend/**", "/frontend/railway.toml"]
