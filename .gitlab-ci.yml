# GitLab CI/CD pipeline for Tiramisu (Hello World Island simulation)
# Java 8, Maven, Spring Boot — build from backend/

stages:
  - build
  - test
  - deploy

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"
  MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version"

cache:
  key: ${CI_COMMIT_REF_SLUG}-maven
  paths:
    - .m2/repository

default:
  image: maven:3.8-eclipse-temurin-8

build:
  stage: build
  script:
    - cd backend && mvn $MAVEN_CLI_OPTS clean package -DskipTests
  artifacts:
    paths:
      - backend/target/*.jar
    expire_in: 1 hour

test:
  stage: test
  script:
    - cd backend && mvn $MAVEN_CLI_OPTS test
  dependencies:
    - build

deploy:
  stage: deploy
  environment: production
  when: manual
  script:
    - echo "Deploying application..."
    - echo "JAR built in build stage (see job artifacts under backend/target)."
    - echo "Configure your deployment (Kubernetes, Docker, etc.) here."
