# API service: connect this repo with Root Directory EMPTY (repo root).
# A second service should use Root Directory "frontend" and frontend/railway.toml.
[build]
builder = "DOCKERFILE"
watchPatterns = ["Dockerfile", "railway.toml", ".dockerignore", "backend/**"]
